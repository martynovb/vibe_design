part of '../reviews.dart';

class ReviewsMobilePage extends StatelessWidget {
  const ReviewsMobilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ReviewsMobilePage'),
    );
  }
}
