part of '../select_level.dart';

class SelectLevelMobilePage extends StatelessWidget {
  const SelectLevelMobilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('SelectLevelMobilePage'),
    );
  }
}
