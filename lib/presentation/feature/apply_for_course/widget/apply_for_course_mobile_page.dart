part of '../apply_for_course.dart';

class ApplyForCourseMobilePage extends StatelessWidget {
  const ApplyForCourseMobilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ApplyForCourseMobilePage'),
    );
  }
}
