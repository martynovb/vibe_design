part of '../home.dart';

class HomeMobilePage extends StatelessWidget {
  const HomeMobilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Home Mobile Page'),
      ),
      body: Center(
        child: Text('Home Mobile Page'),
      ),
    );
  }
}
